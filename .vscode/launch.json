{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Electron: Run App",
      "type": "node",
      "request": "launch",
      "program": "${workspaceFolder}/scripts/start-electron.js",
      "cwd": "${workspaceFolder}",
      "console": "integratedTerminal",
      "skipFiles": [
        "<node_internals>/**"
      ]
    }
  ]
}
